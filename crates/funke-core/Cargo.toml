[package]
name = "funke-core"
version = "0.1.0"
edition = "2021"
description = "UI-free core of the Funke launcher: provider trait, result types, registry"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nucleo-matcher = "0.3"
